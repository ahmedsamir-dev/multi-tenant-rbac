pre-commit:
 commands:
  prettier-format:
   run: pnpm run prettier:format && git add {{staged_files}}
  # lint:
  #  run: pnpm run lint

pre-push:
 commands:
  prettier-format:
    run: pnpm run prettier:format
  lint:
    run: pnpm run lint
